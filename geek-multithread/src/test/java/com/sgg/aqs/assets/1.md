技术题会问：涉及数据库，操作系统，多线程等一系列
考到高并发、高可用的实际应用和数据库深层原理还有jvm底层。
1）数据库深层原理；![20240417131500.png](截屏2024-04-17 13.15.00.png)

2）操作系统；

3）多线程等一系列（高并发）；

一个线程中的多个流程可以获取同一把锁，持有这把同步锁可以再自己可以获取自己的内部锁

隐式锁（即synchronized关键字使用的锁）默认是可重入锁
-synchronized的重入的实现机理 圖
显式锁（即Lock）也有Reentrantlock这样的可重入锁。

一个线程中的多个流程可以获取同一把锁，持有这把同步锁可以再自己可以获取自己的内部锁。


4）jvm底层；

5）高可用实际应用场景。

/*
*在一个synchronized修饰的方法或代码块的内部

* 调用本类的其他synchronized修饰的方法或代码块时，是永远可以得到锁的**
  */
  public class ReEnterLockDemo
  ｛
  static Object
  objectLockA
  = new Object();
  public static void m1()
  new Thread (() -> {
  synchronized (objectLockA){
  system.out.println（Thread.currentThread（）.getName（）+"\t+”-----外层调用“）；
  synchronized (objectLockA){
  System.out.println（Thread.currentThread（）.getName（）+"\t"+”-----中层调用"）；
  synchronized (objectLockA){
  system.out.printI（Thread.currentThread（）.getName（）+"\t"+”---—-内层调用"）；
  }, name: "t1"). start();
  }
